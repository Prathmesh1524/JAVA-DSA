import java.util.Scanner;

//   1.	Find the unique number in a Given Array all other Elements repeated twice
//        5	6 5	4 6	7	4
//        Ans = 7

public class Que1 {
    static int uniqueelement(int []arr){
        int n=arr.length;
        for (int i=0;i<n;i++){
            for (int j=i+1;j<n;j++){
                if (arr[i]==arr[j]){
                    arr[i]=0;
                    arr[j]=0;
                }
            }
        }
        int count=0;
        for (int i=0;i<n;i++){
            if (arr[i]>0){
                count=arr[i];
            }
        }
        return count;
    }
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter the Length of Array  ");
        int x=sc.nextInt();
        int []arr=new int[x];
        System.out.println("Enter the Number in Array ");
        for (int i=0;i< arr.length;i++){
            arr[i]= sc.nextInt();
        }
        System.out.println("The Unique Element is "+ uniqueelement(arr));

    }
}


Que 2.
import java.util.Scanner;
// 	Find the second largest Element in the array
//  8	5	4	8	6	5
//Ans = 6
//

public class Que2 {
//First We are calculating what is the largest Element in the Array .After we get larget then replace it to min value 
static int largest(int []arr){  
    int max=Integer.MIN_VALUE;
    int n=arr.length;
    for (int i=0;i<n;i++){
        if (arr[i]>max){
            max=arr[i];
        }
    }
    return max;
}
 //Traversing the Array agiant so that we Find Second Largest Element
    static int secmax(int []arr){
        int max=largest(arr);
        for (int i=0;i< arr.length;i++){
            if (arr[i]==max){
                arr[i]=Integer.MIN_VALUE; // we are storing the minimum value in max value

            }
        }
        int secondmax=largest(arr);
        return secondmax;
    }
public static void main(String[] args) {
    Scanner sc=new Scanner(System.in);
    System.out.println("Enter The length of Array ");
    int x= sc.nextInt();
    int []arr_1=new int[x];
    System.out.println("Enter the Elements of Array ");
    for (int i=0;i< arr_1.length;i++){
        arr_1[i]= sc.nextInt();
    }
    System.out.println("Second max Element is "+secmax(arr_1));
}
}

